# macOS 的 .command / .sh 必须是 LF 换行 —— 带 CRLF 的话 bash 会报
# 「\r: command not found」或直接闪退，而这两个脚本是比赛当天启动服务用的。
# 仓库在 Windows 上维护、在 Mac 上运行，不锁死就会被 autocrlf 转成 CRLF。
*.command text eol=lf
*.sh      text eol=lf
*.py      text eol=lf
